# FiverrClone

## Build docker image

```sh
    docker buildx build ./ -t 'fiverrclone'
    docker run fiverrclone
```

## Install dependencies

```sh
    pnpm install
```

## Start dev server

```sh
    pnpm -r -stream dev
```
