# FiverrClone

## Build docker image

```console
docker buildx build ./ -t 'fiverrclone'
docker run fiverrclone
```

## Install dependencies

```consolen
pnpm install
```

## Start dev server

```console
pnpm -r -stream dev
```
